# Archived benchmark results — pre-v0.18.1

Historical results, kept for reference only. Measured on the **previous** rig (single
X520 / single X710 / Latitude bnxt) with earlier versions (Runbound v0.16.1 -> v0.17.2,
BIND 9.20.23, Unbound 1.22.0).

The full suite is being **re-run from scratch** with the current stack — Runbound
**v0.18.1** and dnsmark **v2.3.0** — on the new rig: dragonrage <-> dragonsage with
**dual X710 (i40e)** + **X510 (ixgbe)** direct DAC links. New results land in the parent
`docs/benchmark/` directory. Do not cite these numbers as current.
